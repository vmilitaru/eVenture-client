import { makeStyles } from '@material-ui/core/styles'
export const useStyles = makeStyles({
    card: {
        width: '60rem',
        height: 'auto'
    }
    //     linkSpecific: {
    //         textDecoration: 'none'
    //     }
})
